services:
  snsw-ai:
    build: .
    container_name: snsw-ai-container
    volumes:
      - .:/app
      - snsw-models:/root/.cache
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    environment:
      - CUDA_VISIBLE_DEVICES=0
    tty: true
    stdin_open: true

volumes:
  snsw-models:
